plugin Options version 0.1 is

   group run_check : semantic is
       options_check;
   end run_check;

   group generate_spec : code is
       options_generate_spec;
   end generate_spec;

   group generate_body : code is
       options_generate_body;
   end generate_body;