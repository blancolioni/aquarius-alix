class Petal.Checks.Sub_Expression
inherit
   Petal.Context
   Petal.Expression
   
feature

   After_Expression (Child : Petal.Checks.Expression)
      do
         Value := Child.Value
      end

end