plugin Tropos version 0.1 is

   group run_check : semantic is
      check;
   end run_check;
   
  group generate : code is
     generate_config_spec;
--     generate_config_body;
  end generate;

end Tropos;