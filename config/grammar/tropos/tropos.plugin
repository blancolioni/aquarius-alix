plugin Tropos version 0.1 is

   group run_check : semantic is
      after compilation_unit do
         external ("check");
   end run_check;
   
   group generate : code is
      after compilation_unit do
         external ("generate-config-spec");
   end generate;
      