class String_List
   inherit List[String]
   
feature
   Interpolate (Separator : String) : String
      local
         First : Boolean := True
      do
         across Current as S loop
            if First then
               Result := S
               First := False
            else
               Result := Result & Separator & S
            end
         end
      end
end
